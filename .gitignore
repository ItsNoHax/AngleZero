/target
/captures
__pycache__/
*.pyc

# Release archives built by scripts/release.sh
/dist/

# Source car models: tens of megabytes each, and nothing builds from them — the compiled .azcar in
# assets/compiled/ is what ships, and that is committed. See docs/assets.md for where to get them.
/assets/source/*.glb
